## 数据准备 
```c
    ssize_t recv(int sockfd, char* buf, size_t len, int flags);
    int size = recv(sockfd, buf, 1024, 0);
    - 阻塞
        调用IO方法的线程进入阻塞状态
    - 非阻塞
        不会改变线程的状态，通过返回值判断
        size == -1 && errno = EAGAIN 错误或未返回
        size == 0   关闭
        size > 0    返回字符大小
